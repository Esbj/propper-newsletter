import React from 'react';
import './index.css';

class StartPage extends React.Component{
    render(){

        return(
            <div>du behöver logga in!</div>
        )
    }
}

export default StartPage;